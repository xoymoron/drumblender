"""Compatibility entry point; prefer scripts/build_modal_features_new.py.

The former parallel driver and unused Kalman/masking options are retired.
This entry point uses the same explicit options and defaults as the .py driver.
"""

from build_modal_features import main


if __name__ == "__main__":
    main(default_backend="hybrid")
